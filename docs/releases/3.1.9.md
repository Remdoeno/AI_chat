# 3.1.9 聊天输出内部恢复

- 内部分析误入正文时，先重试当前模型一次，再使用本地模型兜底，最多三次文本生成。
- 重试明确要求正式答复并关闭思考；异常前缀不发到浏览器，不显示内部分析检测报错。
- 流式与非流式补救入口采用同一策略，退出/断连关闭生成器。
- 恢复只重做文本生成，不重复执行日程、会员卡等写入。
- 正常回复只调用一次模型；重试次数有上限。所有模型均不可用时仍会如实提示暂时无法生成回复。
