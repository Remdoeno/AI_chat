# 3.0.5：Qwen-Image-2.1 对比试用

- 现有生图模型配置新增独立本地 Qwen-Image-2.1 选项，保留 HiDream 默认设置。
- 支持按用户切换，沿用个人配置保存/恢复机制，不影响文字模型与其他用户。
- 新服务独立环境和进程，环回地址监听；单请求串行推理，繁忙返回明确错误。
- 提供文生图和参考图输入，40步、约2K输出；失败不偷偷回退至HiDream。
- 前端资源版本更新；旺财自我说明同步新增对比试用能力。
- 模型权重采用 Qwen Research License；此次部署供评估对比。
- 远程模型设置与路由单元测试34项通过；上线脚本还要求真实2K文生图和参考图编辑均通过。

回滚：切回 HiDream 即可停止用户请求进入 Qwen。移除新增 provider 和图像服务可完整撤销本功能；原服务及其权重保留。
